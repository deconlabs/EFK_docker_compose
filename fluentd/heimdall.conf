<source>
  @type tail
  path /fluentd/data/MTGOXTOUSD.csv
  tag csvtail
  <parse>
    @type csv
    keys time,host,req_id,user
    time_key time
  </parse>
</source>
<match **>
  @type stdout
</match>